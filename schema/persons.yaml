id: https://example.org/persons-schema
name: persons-schema
description: >-
  Schema for person and country data. Maps relational CSV fields to RDF
  properties using standard vocabularies (schema.org).

prefixes:
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  ex: https://example.org/

default_prefix: ex
default_range: string

imports:
  - linkml:types

classes:
  PersonCollection:
    description: A collection of persons (root container for the dataset).
    tree_root: true
    attributes:
      persons:
        description: The list of persons in this collection.
        range: Person
        multivalued: true
        inlined_as_list: true

  Country:
    description: A country or territory.
    class_uri: schema:Country
    attributes:
      id:
        description: Unique identifier for the country (used as RDF subject URI).
        identifier: true
        range: uriorcurie
      name:
        description: Name of the country.
        slot_uri: schema:name
        required: true
      code:
        description: ISO 3166-1 alpha-3 code of the country.
        slot_uri: schema:identifier

  Person:
    description: A human being.
    class_uri: schema:Person
    attributes:
      id:
        description: Unique identifier for the person (used as RDF subject URI).
        identifier: true
        range: uriorcurie
      name:
        description: Full name of the person.
        slot_uri: schema:name
        required: true
      age:
        description: Age of the person in years.
        slot_uri: schema:age
        range: integer
      email:
        description: Email address of the person.
        slot_uri: schema:email
      country:
        description: Country of origin of the person.
        slot_uri: schema:nationality
        range: Country
